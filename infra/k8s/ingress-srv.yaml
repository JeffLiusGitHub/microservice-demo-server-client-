apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-srv
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: post.com
      http:
        paths:
          - path: /post
            pathType: Prefix
            backend:
              service:
                name: post-clusterip-srv
                port:
                  number: 4000